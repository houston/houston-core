.tickets-list .ticket { 
  background-image: url('<%= "#{Rails.root_url}/images/tree-collapsed.png" %>');
  &.expanded { background-image: url('<%= "#{Rails.root_url}/images/tree-expanded.png" %>'); }
}

.tester-ticket-list {
  .span4:after {
    content: '.';
    background-image: url('<%= "#{Rails.root_url}/images/chevron.png" %>');
    background-position: top right;
    background-repeat: no-repeat;
    position: absolute;
    text-indent: -9999em;
    width: 60px;
    height: 72px;
    top: 0;
    left: -4.5em;
  }
}
