<tr class="ticket {{#firstReleaseAt}}ticket-released{{/firstReleaseAt}} {{#resolved}}ticket-resolved{{/resolved}} {{#closedAt}}ticket-closed{{/closedAt}}" data-ticket-id="{{id}}">
  <td class="ticket-remove">
    <button class="btn btn-danger remove-ticket-button">Remove</button>
  </td>
  <td class="ticket-effort">
    {{estimatedEffort}}
    {{#firstReleaseAt}}
      <i class="icon-ok-sign" rel="tooltip" title="Released {{formatDate this}}"></i>
    {{/firstReleaseAt}}
  </td>
  <td class="ticket-project"><b class="bubble {{projectColor}}"></b> {{projectTitle}}</td>
  <td class="ticket-type ticket-type-{{type}}"></td>
  <td class="ticket-summary">
    {{summary}}
    <a href="{{ticketUrl}}" target="_blank" rel="tooltip" data-tooltip-placement="right" data-original-title="View in {{ticketSystem}}">[#{{number}}]</a>
    {{#tags}}
      <span class="ticket-tag" style="background-color: #{{color}}">{{name}}</span>
    {{/tags}}
    {{#if milestoneName}}
      <span class="ticket-tag" style="background-color: #333">{{milestoneName}}</span>
    {{/if}}
  </td>
  <td class="ticket-worker">
    {{#if checkedOut}}
      {{#if checkedOutByMe}}
        <button class="btn btn-danger check-out-button active" data-toggle="button">Check in</button>
      {{else}}
        Checked out by
        {{{avatar checkedOutBy/email 24 checkedOutBy/name}}}
      {{/if}}
    {{else}}
      <button class="btn btn-info check-out-button" data-toggle="button">Check out</button>
    {{/if}}
  </td>
</tr>
