<% content_for :title do %>
<h1 class="project-banner space-below">
  <%= link_to "Actions", actions_path %>
  <small><%= @action_name %></small>
</h1>
<% end %>

<div class="nomargin">
  <table id="actions" class="table table-sortable table-striped">
    <thead>
      <tr>
        <td class="table-margin"></td>
        <th class="scheduler-action-time">Time</th>
        <th class="scheduler-action-succeded">Succeeded</th>
        <th class="scheduler-action-duration">Duration</th>
        <th class="scheduler-action-exception">Exception</th>
        <td class="table-margin"></td>
      </tr>
    </thead>
    <tbody>
      <% @actions.each do |action| %>
        <tr class="scheduler-action">
          <td class="table-margin"></td>
          <td class="scheduler-action-time"><%= format_time action.started_at %></td>
          <td class="scheduler-action-succeded"><%= format_action_state action %></td>
          <td class="scheduler-action-duration"><%= format_duration action.duration %></td>
          <td class="scheduler-action-exception"><%= action.error.message if action.error %><td>
          <td class="table-margin"></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<% content_for :javascripts do %>
  <script type="text/javascript">
    $(function() {
      $('#actions').tablesorter();
    });
  </script>
<% end %>
