<%= header do |p| %>
  <%= p.breadcrumbs "Projects" => projects_path,
                    @project.name => Project.all,
                    @environment.name => @project.environments %>
<% end %>

<%= render :partial => 'form' %>
