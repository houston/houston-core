<%= header do |p| %>
  <%= p.breadcrumbs "Projects" => projects_path,
                  @project.name => Project.all,
                  "Environments" => nil %>
  <%= p.actions do %>
    <%= link_to 'New', new_project_environment_path(@project), :class => 'btn btn-primary' %>
  <% end %>
<% end %>

<%= render :partial => "table" %>
