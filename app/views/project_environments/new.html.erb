<%= header do |p| %>
  <%= p.breadcrumbs "Projects" => projects_path,
                    @project.name => Project.all,
                    "New" => nil %>
<% end %>

<%= render :partial => 'form' %>
